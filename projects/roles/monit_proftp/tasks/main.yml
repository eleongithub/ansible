---
# tasks file for monit_proftp

- name: Copy configuration file
  template:
    backup: yes
    src: proftpd.conf.j2
    dest: "{{ monit_proftp_config_file }}"
    owner: root
    group: root
    mode: 0644

- name: Reload monit
  command: echo "Reload monit..."
  notify:
    - Reload monit